% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saint-fit.R
\name{brulee_saint}
\alias{brulee_saint}
\alias{brulee_saint.default}
\alias{brulee_saint.data.frame}
\alias{brulee_saint.matrix}
\alias{brulee_saint.formula}
\alias{brulee_saint.recipe}
\title{Fit SAINT models for tabular data}
\usage{
brulee_saint(x, ...)

\method{brulee_saint}{default}(x, ...)

\method{brulee_saint}{data.frame}(
  x,
  y,
  epochs = 100L,
  num_embedding = 32L,
  attention_type = "colrow",
  num_attn_heads = 8L,
  num_attn_blocks = 6L,
  dropout_attn = 0.1,
  dropout_ff = 0.1,
  dropout = 0,
  hidden_units = NULL,
  hidden_activations = NULL,
  penalty = 0.001,
  mixture = 0,
  validation = 0.1,
  optimizer = "ADAMw",
  learn_rate = 1e-04,
  rate_schedule = "none",
  momentum = 0,
  batch_size = NULL,
  class_weights = NULL,
  stop_iter = 5,
  verbose = FALSE,
  device = NULL,
  ...
)

\method{brulee_saint}{matrix}(
  x,
  y,
  epochs = 100L,
  num_embedding = 32L,
  attention_type = "colrow",
  num_attn_heads = 8L,
  num_attn_blocks = 6L,
  dropout_attn = 0.1,
  dropout_ff = 0.1,
  dropout = 0,
  hidden_units = NULL,
  hidden_activations = NULL,
  penalty = 0.001,
  mixture = 0,
  validation = 0.1,
  optimizer = "ADAMw",
  learn_rate = 1e-04,
  rate_schedule = "none",
  momentum = 0,
  batch_size = NULL,
  class_weights = NULL,
  stop_iter = 5,
  verbose = FALSE,
  device = NULL,
  ...
)

\method{brulee_saint}{formula}(
  formula,
  data,
  epochs = 100L,
  num_embedding = 32L,
  attention_type = "colrow",
  num_attn_heads = 8L,
  num_attn_blocks = 6L,
  dropout_attn = 0.1,
  dropout_ff = 0.1,
  dropout = 0,
  hidden_units = NULL,
  hidden_activations = NULL,
  penalty = 0.001,
  mixture = 0,
  validation = 0.1,
  optimizer = "ADAMw",
  learn_rate = 1e-04,
  rate_schedule = "none",
  momentum = 0,
  batch_size = NULL,
  class_weights = NULL,
  stop_iter = 5,
  verbose = FALSE,
  device = NULL,
  ...
)

\method{brulee_saint}{recipe}(
  x,
  data,
  epochs = 100L,
  num_embedding = 32L,
  attention_type = "colrow",
  num_attn_heads = 8L,
  num_attn_blocks = 6L,
  dropout_attn = 0.1,
  dropout_ff = 0.1,
  dropout = 0,
  hidden_units = NULL,
  hidden_activations = NULL,
  penalty = 0.001,
  mixture = 0,
  validation = 0.1,
  optimizer = "ADAMw",
  learn_rate = 1e-04,
  rate_schedule = "none",
  momentum = 0,
  batch_size = NULL,
  class_weights = NULL,
  stop_iter = 5,
  verbose = FALSE,
  device = NULL,
  ...
)
}
\arguments{
\item{x}{Depending on the context:
\itemize{
\item A \strong{data frame} of predictors.
\item A \strong{matrix} of predictors.
\item A \strong{recipe} specifying a set of preprocessing steps
created from \code{\link[recipes:recipe]{recipes::recipe()}}.
}

The predictor data should be standardized (e.g. centered or scaled).}

\item{...}{Options to pass to the learning rate schedulers via
\code{\link[=set_learn_rate]{set_learn_rate()}}. For example, the \code{reduction} or \code{steps} arguments to
\code{\link[=schedule_step]{schedule_step()}} could be passed here.}

\item{y}{When \code{x} is a \strong{data frame} or \strong{matrix}, \code{y} is the outcome
specified as:
\itemize{
\item A \strong{data frame} with 1 column (numeric or factor).
\item A \strong{matrix} with numeric column  (numeric or factor).
\item A  \strong{vector}  (numeric or factor).
}}

\item{epochs}{An integer for the number of epochs of training.}

\item{num_embedding}{An integer for the embedding dimension. Each feature
(categorical or continuous) is mapped to a vector of this dimension.
Must be >= 1.}

\item{attention_type}{A character string for the type of attention to use.
Options are:
\itemize{
\item \code{"col"}: Column attention only (attends across features). This is the
SAINT-s variant.
\item \code{"row"}: Row/intersample attention only (attends across samples within
a batch). This is the SAINT-i variant.
\item \code{"colrow"}: Alternates between column and row attention in each
transformer block. This is the full SAINT model.
}}

\item{num_attn_heads}{An integer for the number of attention heads for
column attention. Must be >= 1.}

\item{num_attn_blocks}{An integer for the number of transformer blocks
(depth). Must be >= 1.}

\item{dropout_attn}{A number in \verb{[0, 1)} for the dropout rate applied to
attention weights during training.}

\item{dropout_ff}{A number in \verb{[0, 1)} for the dropout rate applied within
the feedforward layers.}

\item{dropout}{A number in \verb{[0, 1)} for the dropout rate applied between
the last hidden layer and the output head. Only has effect when
\code{hidden_units} is not \code{NULL}. Default is 0 (no dropout).}

\item{hidden_units}{An integer vector for the number of units in optional
hidden layers between the transformer backbone and the output head.
When \code{NULL} (the default), no hidden layers are added and the flattened
transformer output is projected directly to the output.}

\item{hidden_activations}{A character vector of activation functions for the
hidden layers. Must be the same length as \code{hidden_units} or a single value
that will be recycled. See \code{\link[=brulee_activations]{brulee_activations()}} for options.}

\item{penalty}{The amount of weight decay (i.e., L2 regularization).}

\item{mixture}{Proportion of Lasso Penalty (type: double, default: 0.0). A
value of mixture = 1 corresponds to a pure lasso model, while mixture = 0
indicates ridge regression (a.k.a weight decay). Must be zero for
optimizers \code{"ADAMw"}, \code{"RMSprop"}, \code{"Adadelta"}.}

\item{validation}{The proportion of the data randomly assigned to a
validation set.}

\item{optimizer}{The method used in the optimization procedure. Possible choices
are \code{"SGD"},  \code{"ADAMw"}, \code{"Adadelta"}, \code{"Adagrad"}, \code{"RMSprop"}, and
\code{"LBFGS"}. \code{"LBFGS"} is the only second-order method and does not use
batches.}

\item{learn_rate}{A positive number that controls the initial rapidity that
the model moves along the descent path. Values around 0.1 or less are
typical.}

\item{rate_schedule}{A single character value for how the learning rate
should change as the optimization proceeds. Possible values are
\code{"none"} (the default), \code{"decay_time"}, \code{"decay_expo"}, \code{"cyclic"} and
\code{"step"}. See \code{\link[=schedule_decay_time]{schedule_decay_time()}} for more details.}

\item{momentum}{A positive number usually on \verb{[0.50, 0.99]} for the momentum
parameter in gradient descent.  (optimizers \code{"SGD"},  and \code{"RMSprop"} only,
ignored otherwise).}

\item{batch_size}{An integer for the number of training set points in each
batch. (\code{optimizer != "LBFGS"} only, ignored otherwise)}

\item{class_weights}{Numeric class weights (classification only). The value
can be:
\itemize{
\item A named numeric vector (in any order) where the names are the outcome
factor levels.
\item An unnamed numeric vector assumed to be in the same order as the outcome
factor levels.
\item A single numeric value for the least frequent class in the training data
and all other classes receive a weight of one.
}}

\item{stop_iter}{A non-negative integer for how many iterations with no
improvement before stopping.}

\item{verbose}{A logical that prints out the iteration history.}

\item{device}{A single character string for the device to train on (e.g.,
\code{"cpu"} or \code{"cuda"} for GPU). If \code{NULL}, the function will use the GPU if
available, otherwise CPU. See \link{training_efficiency}.}

\item{formula}{A formula specifying the outcome term(s) on the left-hand side,
and the predictor term(s) on the right-hand side.}

\item{data}{When a \strong{recipe} or \strong{formula} is used, \code{data} is specified as:
\itemize{
\item A \strong{data frame} containing both the predictors and the outcome.
}}
}
\value{
A \code{brulee_saint} object with elements:
\itemize{
\item \code{models_obj}: a serialized raw vector for the torch module.
\item \code{estimates}: a list of matrices with the model parameter estimates per
epoch.
\item \code{best_epoch}: an integer for the epoch with the smallest loss.
\item \code{loss}: A vector of loss values (MSE for regression, negative log-
likelihood for classification) at each epoch.
\item \code{dim}: A list of data dimensions and feature metadata.
\item \code{y_stats}: A list of summary statistics for numeric outcomes.
\item \code{parameters}: A list of some tuning parameter values.
\item \code{device}: A character string for the device used during training.
\item \code{blueprint}: The \code{hardhat} blueprint data.
}
}
\description{
\code{brulee_saint()} fits the SAINT (Self-Attention and Intersample Attention
Transformer) model from Somepalli \emph{et al} (2021). SAINT applies multi-head
self-attention across both features (column attention) and samples within a
batch (row/intersample attention) to learn complex feature interactions.
}
\details{
\subsection{Architecture}{

The SAINT architecture has three stages:
\enumerate{
\item \strong{Embedding layer}: Categorical features are mapped through per-feature
embedding tables. Continuous features are passed through per-feature MLPs
(1 -> 100 -> \code{num_embedding}).
\item \strong{Transformer backbone}: A stack of \code{num_attn_blocks} transformer layers.
Each layer contains multi-head self-attention followed by a feedforward
network with GEGLU activation. For \code{attention_type = "colrow"}, each block
alternates between column attention (across features) and row attention
(across samples within the batch).
\item \strong{Output head}: Flattens the transformer output and projects through
optional hidden layers to the output dimension.
}
}

\subsection{Attention Types}{
\itemize{
\item \strong{Column attention} (\code{"col"}): Standard self-attention over features.
Each feature embedding attends to all other feature embeddings.
\item \strong{Row attention} (\code{"row"}): Intersample attention. Reshapes the batch so
that each sample's full feature representation becomes a single token,
then applies attention across all samples in the batch.
\item \strong{Column-row attention} (\code{"colrow"}): Alternates between column and row
attention in each transformer block. This is the full SAINT model.
}
}

\subsection{Learning Rates}{

The learning rate can be set to constant (the default) or dynamically set
via a learning rate scheduler (via the \code{rate_schedule}). Using
\code{rate_schedule = 'none'} uses the \code{learn_rate} argument.
}

\subsection{Other Notes}{

Unlike other \pkg{brulee} models, \code{brulee_saint()} natively handles factor
predictors via learned embeddings. Factor columns are automatically detected
and embedded, while numeric columns pass through per-feature MLPs. There is
\emph{no need to pre-encode factors as indicators}.

When the outcome is a number, the function internally standardizes the
outcome data to have mean zero and a standard deviation of one. The prediction
function creates predictions on the original scale.

By default, training halts when the validation loss increases for at least
\code{stop_iter} iterations. If \code{validation = 0} the training set loss is used.

The model objects are saved for each epoch so that the number of epochs can
be efficiently tuned. The \code{\link[=predict]{predict()}} method for this model has an
\code{epoch} argument (which defaults to the epoch with the best loss value).
}
}
\examples{
\dontshow{if (!brulee:::is_cran_check()) withAutoprint(\{ # examplesIf}
\donttest{
pkgs <- c("recipes", "yardstick", "modeldata")
if (torch::torch_is_installed() & rlang::is_installed(pkgs)) {

  set.seed(87261)
  tr_data <- modeldata::sim_regression(500)
  te_data <- modeldata::sim_regression(50)

  set.seed(2)
  fit <- brulee_saint(outcome ~ ., data = tr_data,
                      epochs = 50L, batch_size = 64L, stop_iter = 10L,
                      learn_rate = 0.001)
  fit

  autoplot(fit)

  library(yardstick)
  predict(fit, te_data) |>
   dplyr::bind_cols(te_data) |>
   rmse(outcome, .pred)

}
}
\dontshow{\}) # examplesIf}
}
\references{
Somepalli, G., Goldblum, M., Schwarzschild, A., Bruss, C. B., &
Goldstein, T. (2021). SAINT: Improved Neural Networks for Tabular Data
via Row Attention and Contrastive Pre-Training. arXiv preprint
arXiv:2106.01342.
}
\seealso{
\code{\link[=predict.brulee_saint]{predict.brulee_saint()}}, \code{\link[=autoplot.brulee_saint]{autoplot.brulee_saint()}}
}
