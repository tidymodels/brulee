% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabicl-download.R
\name{tab_icl_download_weights}
\alias{tab_icl_download_weights}
\alias{tab_icl_weights_available}
\title{Download and cache pretrained TabICL weights}
\usage{
tab_icl_download_weights(
  task = c("classification", "regression"),
  version = tabicl_default_version(),
  date = tabicl_default_date(),
  repo = tabicl_default_repo(),
  cache_dir = tabicl_cache_dir(),
  call = rlang::caller_env()
)

tab_icl_weights_available(
  task = c("classification", "regression"),
  cache_dir = tabicl_cache_dir()
)
}
\arguments{
\item{task}{The task(s) to act on, one or both of \code{"classification"} and
\code{"regression"}. Both are fetched (and checked) by default.}

\item{version, date}{The release to fetch, identifying the tag
\verb{<version>-<date>} (for example \code{"v2"} and \code{"2026-02-12"}).}

\item{repo}{The \code{owner/name} of the GitHub repository hosting the weights.}

\item{cache_dir}{The root of the local weight cache. Defaults to the
\code{brulee.tabicl_cache_dir} option or \verb{~/.cache/TabICL}.}

\item{call}{The calling environment, used for error messages.}
}
\value{
\code{tab_icl_download_weights()} invisibly returns the populated
\verb{<cache_dir>/<version>/<date>} directory. \code{tab_icl_weights_available()}
returns a single logical.
}
\description{
\code{\link[=brulee_tab_icl]{brulee_tab_icl()}} needs pretrained weights that are not shipped with the
package. \code{tab_icl_download_weights()} fetches them from a release of the
\code{tidymodels/tabicl-weights} GitHub repository into the local cache.
\code{tab_icl_weights_available()} reports whether the cache already holds usable
weights.
}
\details{
Each release carries the two files brulee reads per task (a JSON config and a
safetensors weight file) as individual assets. They are downloaded into
\verb{<cache_dir>/<version>/<date>/<TaskLabel>/}. A file already present and
complete is left in place, so re-running resumes rather than re-downloads.

The cache location can be overridden with the \code{brulee.tabicl_cache_dir}
option. When brulee is attached and the weights are missing, the package
offers to download them in interactive sessions and downloads them otherwise;
set \code{options(brulee.tabicl_autodownload = FALSE)} to disable that behavior.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
tab_icl_download_weights()
tab_icl_weights_available()
\dontshow{\}) # examplesIf}
}
