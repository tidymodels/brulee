# Python venv for the spike (recreate with: python -m venv .venv)
.venv/

# Large released weights, re-derivable by running convert_ckpt.py.
# Stored under artifacts/<version>/<date>/<TaskLabel>/. The small companion
# files (config.json, manifest.json, state_dict_keys.txt, golden/) are tracked.
artifacts/**/*model.safetensors
__pycache__/
.DS_Store
