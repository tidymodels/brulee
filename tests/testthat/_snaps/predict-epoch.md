# brulee_mlp() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_linear_reg() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_logistic_reg() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_multinomial_reg() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_resnet() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_rln() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_saint() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

# brulee_auto_int() clamps an out-of-range epoch

    Code
      boundary <- predict(fit, new_data, epoch = length(fit$estimates))
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 4.

---

    Code
      past_end <- predict(fit, new_data, epoch = 10L)
    Condition
      Warning in `predict()`:
      The model was fit for 3 epochs; the last epoch is used instead of epoch 10.

